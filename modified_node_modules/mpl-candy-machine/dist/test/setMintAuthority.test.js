"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const umi_1 = require("@metaplex-foundation/umi");
const ava_1 = __importDefault(require("ava"));
const src_1 = require("../src");
const _setup_1 = require("./_setup");
(0, ava_1.default)('it can update the mint authority of a candy machine v1', async (t) => {
    // Given an Candy Machine with a mint authority equal to its authority A.
    const umi = await (0, _setup_1.createUmi)();
    const authorityA = (0, umi_1.generateSigner)(umi);
    const candyMachine = await (0, _setup_1.createV1)(umi, {
        authority: authorityA.publicKey,
    });
    const { mintAuthority: mintAuthorityA } = await (0, src_1.fetchCandyMachine)(umi, candyMachine.publicKey);
    t.deepEqual(mintAuthorityA, authorityA.publicKey);
    // When we update its mint authority.
    const mintAuthorityB = (0, umi_1.generateSigner)(umi);
    await (0, umi_1.transactionBuilder)()
        .add((0, src_1.setMintAuthority)(umi, {
        candyMachine: candyMachine.publicKey,
        authority: authorityA,
        mintAuthority: mintAuthorityB,
    }))
        .sendAndConfirm(umi);
    // Then the Candy Machine's mint authority was updated accordingly.
    const candyMachineAccount = await (0, src_1.fetchCandyMachine)(umi, candyMachine.publicKey);
    t.like(candyMachineAccount, {
        authority: (0, umi_1.publicKey)(authorityA.publicKey),
        mintAuthority: (0, umi_1.publicKey)(mintAuthorityB.publicKey),
    });
});
(0, ava_1.default)('it can update the mint authority of a candy machine v2', async (t) => {
    // Given an Candy Machine with a mint authority equal to its authority A.
    const umi = await (0, _setup_1.createUmi)();
    const authorityA = (0, umi_1.generateSigner)(umi);
    const candyMachine = await (0, _setup_1.createV2)(umi, {
        authority: authorityA.publicKey,
    });
    const { mintAuthority: mintAuthorityA } = await (0, src_1.fetchCandyMachine)(umi, candyMachine.publicKey);
    t.deepEqual(mintAuthorityA, authorityA.publicKey);
    // When we update its mint authority.
    const mintAuthorityB = (0, umi_1.generateSigner)(umi);
    await (0, umi_1.transactionBuilder)()
        .add((0, src_1.setMintAuthority)(umi, {
        candyMachine: candyMachine.publicKey,
        authority: authorityA,
        mintAuthority: mintAuthorityB,
    }))
        .sendAndConfirm(umi);
    // Then the Candy Machine's mint authority was updated accordingly.
    const candyMachineAccount = await (0, src_1.fetchCandyMachine)(umi, candyMachine.publicKey);
    t.like(candyMachineAccount, {
        authority: (0, umi_1.publicKey)(authorityA.publicKey),
        mintAuthority: (0, umi_1.publicKey)(mintAuthorityB.publicKey),
    });
});
//# sourceMappingURL=setMintAuthority.test.js.map